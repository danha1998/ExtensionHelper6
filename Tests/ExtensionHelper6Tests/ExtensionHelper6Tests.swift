import XCTest
@testable import ExtensionHelper6

final class ExtensionHelper6Tests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ExtensionHelper6().text, "Hello, World!")
    }
}
